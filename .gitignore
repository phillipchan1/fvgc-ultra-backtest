data/raw/glbx-mdp3-20231002-20251027.ohlcv-1s.csv
data/raw/*.ohlcv-1s.csv
# Consolidated NQ front-month files are rebuilt locally by tools/update_data.py
# (or tools/consolidate_data.py for a full pass). They're large binaries and
# should never live in git.
data/consolidated/
__pycache__/
*.pyc
logs/
.DS_Store

# Local secrets. Use .env.example as the template. GitHub Actions
# stores the same keys as repo secrets so CI keeps working.
.env
.env.local

# Claude Code agent worktrees — local scratch copies, never commit
.claude/worktrees/

# Node deps (Cloudflare worker)
node_modules/
