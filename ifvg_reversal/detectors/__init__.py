"""Detectors for the IFVG reversal pipeline.

Each detector is independent and emits typed events keyed by candle timestamp.
The model (ifvg_reversal/model.py) composes them per §1 procedural order.
"""
