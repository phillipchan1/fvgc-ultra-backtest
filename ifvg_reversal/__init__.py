"""IFVG Reversal — liquidity-sweep reversal model (sibling of fvgc/, not a variant of it).

Spec: ifvg_reversal/SPEC.md  (keep __version__ in sync with the spec's Version field).
Do NOT modify model behavior without a corresponding SPEC.md bump + changelog entry.

Shares data/, levels/, and tools/ with fvgc/ but has its own model, levels list,
and study folder prefix (studies/ifvg_reversal_*).
"""

__version__ = "1.0.0"
