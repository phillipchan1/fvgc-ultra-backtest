{
  "name": "nq-live-snapshot",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that polls Databento and caches NQ snapshot for the briefing dashboard",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test:live": "curl -s https://nq-live-snapshot.${CF_SUBDOMAIN:-your-subdomain}.workers.dev/api/live | jq"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
